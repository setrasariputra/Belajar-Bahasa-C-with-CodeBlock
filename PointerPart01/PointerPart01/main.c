#include <stdio.h>

int main()
{
    int number = 14026;

    // untuk mencetak nilai
    printf("Content of number = %d\n", number);
    // untuk menyimpan nilai di memori
    printf("Address of number = %p\n", &number);

    return 0;
}
